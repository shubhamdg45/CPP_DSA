// number is divisible by 5 & 3 but not 15
#include<iostream>
using namespace std;
int main1(){
    int n;
    cin>>n;
   // if((n%5==0 || n%3==0) && (n%15!=0)){
       // cout<<" num is divisible by 5 &3 but not by 15";

   // }
   // else{
      //  cout<<"No";
   // }
   (n%3==0 && n%15!=0 || n%5==0) ? cout<<"divsible":cout<<" not";
return 0;
} 


int main5(){
    if(cout<<"hello" || cout<<"world"){
        cout<<"shubham";
    }

    // int x = cout<<"shubham Giram";
    // cout<<x;
    return 0;
}

int main8(){
    int i;
    for(i = 4; i>=0; i--);
    cout<<i;
}

int main3(){
    if(cout<<"hello" && cout<<"world"){

    }
}

int main(){
    // int i = 4;
    // cout<<i++;
    // int j = ++i;
    // cout<<j;

    if(1)
    cout<<"true";
    cout<<"false";
}

